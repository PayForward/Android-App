<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.payforward.androidapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SetLocationActivity"
            android:label="@string/title_activity_set_location" />
        <activity android:name=".SignUpTypeActivity" />
        <activity android:name=".DescriptionActivity" />
        <activity android:name=".post.PostTypeActivity" />
        <activity android:name=".post.CategoryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".post.SubCategoryActivity" />
        <activity
            android:name=".NameBoardActivity"
            android:label="@string/title_activity_name_board"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".termsActivity" />
        <activity android:name=".createAccountActivity" />
        <activity android:name=".verifyMemberActivity" />
        <activity android:name=".verificationCodeActivity" />
        <activity android:name=".UploadPhotoActivity"></activity>
    </application>

</manifest>